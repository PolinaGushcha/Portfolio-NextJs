class DASHBOARD {
  private root = '/i'

  HOME = this.root
  EXPERIENCE = `${this.root}/experience`
  SKILLS = `${this.root}/skills`
  WORKS = `${this.root}/works`
  CONTACTS = `${this.root}/contacts`
}

export const DASHBOARD_PAGES = new DASHBOARD()
